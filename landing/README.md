# JobRadar — landing page

A single-page Next.js showcase for [JobRadar](../README.md), a Python CLI
that scrapes job listings, scores them against a configurable skills
profile, and exports a ranked shortlist to CSV.

## Develop

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```
